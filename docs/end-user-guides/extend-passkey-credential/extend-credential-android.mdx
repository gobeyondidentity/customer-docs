---
title: How to Extend Your Passkey Credential to an Android Device
id: extend-credential-android
description: 'Learn how to extend your passkey to a new Android device.'
slug: /end-user-guides/extend-passkey-credential/extend-credential-android
keywords:
  - Extend passkey credential to Android
  - Passkey
pagination_next: null
pagination_prev: null
last_update:
  date: August 7, 2025
  author: Dolores M. Bernal
doc_type: overview
draft: false
displayed_sidebar: mainSidebar
---

### Overview

This guide is for users who have already registered a Beyond Identity passkey and want to set it up on an Android device, such as a Samsung Galaxy or another Android phone.

Completing this process creates a backup of your passkey, making it easier to restore if it’s ever removed from the original device.

<br/>
---

### What You'll Learn

You’ll learn how to add or extend your passkey credential to a trusted Android device in a few simple steps.

<br/>
---
 
### Steps

1. If you haven’t already, download the **Beyond Identity Authenticator** on your Android device from the Google Play Store, or by visiting https://app.byndid.com/downloads in your mobile browser.

<br/>
- **Google Play Store View**

<img
  src={require('../images/1-1-android-extend.JPEG').default}
   alt="Authenticator"
      style={{
        maxHeight: '400px',
        width: 'auto',
        objectFit: 'contain',
        display: 'block',
        marginLeft: '2rem',
        marginTop: '1rem',
        marginBottom: '1rem'
      }}
/>


<br/>
- **Browser View**

<img
  src={require('../images/1-2-android-extend.png').default}
   alt="Authenticator"
      style={{
        maxHeight: '500px',
        width: 'auto',
        objectFit: 'contain',
        display: 'block',
        marginLeft: '2rem',
        marginTop: '1rem',
        marginBottom: '1rem'
      }}
/>

2. Once installed, open the Beyond Identity Authenticator on your Android device. Then, tap **Next**. 

<img
  src={require('../images/2-android-extend.png').default}
   alt="Authenticator"
      style={{
        maxHeight: '500px',
        width: 'auto',
        objectFit: 'contain',
        display: 'block',
        marginLeft: '2rem',
        marginTop: '1rem',
        marginBottom: '1rem'
      }}
/>

3. Tap **Enroll with Existing Device**. 

<img
  src={require('../images/3-android-extend.png').default}
   alt="Authenticator"
      style={{
        maxHeight: '500px',
        width: 'auto',
        objectFit: 'contain',
        display: 'block',
        marginLeft: '2rem',
        marginTop: '1rem',
        marginBottom: '1rem'
      }}
/>

4. Tap **Generate QR / 9 Digit Code**.

<img
  src={require('../images/android-step-code.png').default}
   alt="Authenticator"
      style={{
        maxHeight: '500px',
        width: 'auto',
        objectFit: 'contain',
        display: 'block',
        marginLeft: '2rem',
        marginTop: '1rem',
        marginBottom: '1rem'
      }}
/> 
  
  The next screen will prompt you to enter the 9-digit code displayed on the device that already has the Authenticator and passkey. After entering the code, the passkey will be extended to your Android smartphone. 

<img
  src={require('../images/enter-code-from-device-android.png').default}
   alt="Authenticator"
      style={{
        maxHeight: '500px',
        width: 'auto',
        objectFit: 'contain',
        display: 'block',
        marginLeft: '2rem',
        marginTop: '1rem',
        marginBottom: '1rem'
      }}
/> 


5. In an example use case, let's extend your passkey from a Windows device to your Android phone.

      First, open the Beyond Identity Authenticator in your Windows machine, then click **Set up other devices**. 

<img
  src={require('../images/6-android-extend.png').default}
   alt="Authenticator"
      style={{
        maxHeight: '500px',
        width: 'auto',
        objectFit: 'contain',
        display: 'block',
        marginLeft: '2rem',
        marginTop: '1rem',
        marginBottom: '1rem'
      }}
/>

6. Read the message in the dialog window. If you choose to extend the passkey, click **Yes, extend my passkey**. 

<img
  src={require('../images/7-android-extend.png').default}
   alt="Authenticator"
      style={{
        maxHeight: '500px',
        width: 'auto',
        objectFit: 'contain',
        display: 'block',
        marginLeft: '2rem',
        marginTop: '1rem',
        marginBottom: '1rem'
      }}
/>

<br/>

7. When the 9-digit code that appears on the next screen to your Android device.

<img
  src={require('../images/extend-android-enter-code.png').default}
   alt="Authenticator"
      style={{
        maxHeight: '500px',
        width: 'auto',
        objectFit: 'contain',
        display: 'block',
        marginLeft: '2rem',
        marginTop: '1rem',
        marginBottom: '1rem'
      }}
/>

8. When the code is entered successfully, a green confirmation banner will appear indicating that the passkey was added to your Android device.


<img
  src={require('../images/8-android-extend.png').default}
   alt="Authenticator"
      style={{
        maxHeight: '500px',
        width: 'auto',
        objectFit: 'contain',
        display: 'block',
        marginLeft: '2rem',
        marginTop: '1rem',
        marginBottom: '1rem'
      }}
/>

That's it! You have completed extending your passkey to an Android device. If you encounter any issues after several attempts, please contact your IT administrator. 

<br/>
<br/>
<br/>